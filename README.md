# mini-overleaf
Browser-based LaTeX editor with live PDF preview and DOCX export (FastAPI + XeLaTeX + CodeMirror). Includes structured error parsing, engine selection, and Docker-based deployment for self-hosted Overleaf-style workflows.
